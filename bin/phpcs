#!/bin/bash
set -eu -o pipefail

readonly DOCKER=$(command -v podman || command -v docker)
$DOCKER run --name phpcs$$ --rm --security-opt label=disable -v "$PWD":/app phpqa/phpcs phpcs --standard=PSR12 "$@"
